#ifndef BENCHMARK_H
#define BENCHMARK_H

// Прогоняет двоичную сортировку на трёх наборах данных одинакового размера:
//  - уже отсортированный (худший случай: дерево вырождается в цепочку);
//  - инвертированный (обратный порядок, тоже худший случай);
//  - случайный (средний случай).
// Для каждого набора выводит время работы и число сравнений.
void runBenchmark(int size);

#endif
